name: Build Static Export

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install

      - name: Build Next.js
        env:
          NEXT_PUBLIC_API_URL: https://ai-meta-factory.onrender.com
        run: npm run build

      - name: Upload out folder
        uses: actions/upload-artifact@v4
        with:
          name: next-out
          path: out
          retention-days: 7
